# Elchi

A lightweight web board connecting travelers between Korea and Uzbekistan with people who need something delivered.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
